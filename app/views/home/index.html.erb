
<div class="<page-header"> 
  
<h1>Connect4 <small>You Rock!</small></h1>
<p>Click the -drop- links to start the game. Default player plays blue.</p>

</div>
<div></div>

 <table class="table-condensed">
            <thead>
               
                <% for i in 0..6 do %>
                    <th>
                        <a class="drop" data-col="<%= i; %>" href="#">drop</a>
                    </th>
                <% end %>
            </thead>
            <tbody>
                <!-- Example row -->
                <% 
                k=0;
                for i in 0..5 do %>
                    <tr>
                        <% for j in 0..6 do
                        k+=1
                        %>
                            <td class="empty" id="<%= k; %>" data-col="<%= j; %>">
                                &nbsp;
                            </td>
                        <% end %>
                    </tr>
                <% end %>
            </tbody>
        </table>